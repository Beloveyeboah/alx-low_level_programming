#include "3-calc.h"

/**
 * op_add - op_add prints
 * @a: the
 * @b: the
 *
 * Return: 0
 */
int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - op_sub write
 * @a: the
 * @b: the
 * Return: 0
 */
int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - op_mul prints
 * @a: the
 * @b: the
 * Return: 0
 */
int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - op_div prints
 * @a: the
 * @b: the
 * Return: 0
 */
int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod - op_mod the printd
 * @a: the
 * @b: the
 * Return: 1
 */
int op_mod(int a, int b)
{
	return (a % b);
}
