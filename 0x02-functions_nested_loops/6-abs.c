#include "main.h"

/**
 *_abs- compute the absolute value of an integer

 *@n:the int to check
 * click: the int
 *
 * Return: the absolute
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
